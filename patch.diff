--- app/src/main/java/com/music/bitchord/playback/PlaybackService.kt
+++ app/src/main/java/com/music/bitchord/playback/PlaybackService.kt
@@ -114,7 +114,7 @@
  */
 class PlaybackService : MediaSessionService() {

-    private var mediaSession: MediaSession? = null
+    var mediaSession: MediaSession? = null

     /**
      * The player the session is on. Swaps with [spare] at every crossfade — see
@@ -899,6 +899,7 @@
             .setSessionActivity(sessionActivity())
             .setCallback(sessionCallback)
             .build()
+        Companion.mediaSession = mediaSession
         mediaSession?.setCustomLayout(notificationButtons())
         // Required for Google Assistant (and automotive/wear OS) to index the
         // library if this service ever upgrades to MediaLibraryService. As a
@@ -3296,6 +3297,7 @@
         crossfade = null
         mediaSession?.release()
         mediaSession = null
+        Companion.mediaSession = null
         player?.removeListener(playbackListener)
         player?.removeAnalyticsListener(formatListener)
         player?.release()
@@ -3587,6 +3589,7 @@
         const val DISCORD_TEARDOWN_TIMEOUT_MS = 3_000L

         /**
+         */
+        var mediaSession: MediaSession? = null
+
+        /**
          * Size of each range the player fetches. The same figure read-ahead
